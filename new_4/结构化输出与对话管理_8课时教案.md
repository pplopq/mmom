# 结构化输出与对话管理 · 多角色对话系统
## 8 课时完整教案

**课程定位**：W4 大模型 API 调用模块核心课，面向已掌握 Python 基础与 Qwen API 调用的学员
**课时总量**：8 × 45 分钟
**先修要求**：Python 基础语法、JSON/字典操作、Qwen API Key 配置完成
**核心目标**：学会控制 LLM 输出格式（JSON/XML），实现多角色设定与有状态上下文管理

---

## 课程总体安排

| 课时 | 主题 | 知识类型 | 课时目标 |
|------|------|---------|---------|
| 第1课 | JSON 结构化输出基础 | 原理+实战 | 理解为何需要结构化输出，掌握 prompt 诱导方法 |
| 第2课 | JSON Schema 约束与校验 | 进阶技巧 | 用 Schema 精确约束 AI 输出字段 |
| 第3课 | XML 结构化输出 | 扩展应用 | 掌握 XML 格式输出及解析 |
| 第4课 | System Prompt 与角色设定 | 核心概念 | 理解三种消息角色，设计持久人设 |
| 第5课 | 多轮对话与上下文管理 | 核心技术 | 实现有状态的连续对话 |
| 第6课 | 多角色对话系统设计 | 综合项目 | 构建可切换角色的对话系统 |
| 第7课 | 对话记忆与摘要压缩 | 进阶优化 | 解决长对话 token 超限问题 |
| 第8课 | 综合项目：AI 助理平台 | 项目实战 | 整合全部技能，完成完整项目 |

---

## 第 1 课：JSON 结构化输出基础

**课时**：45 分钟
**目标**：理解结构化输出的价值，能通过 Prompt 引导 AI 输出标准 JSON

### 1.1 导入：为什么需要结构化输出（5 分钟）

**类比引入**：
> 你去便利店买东西，收银员说"八块五"和打印一张小票，哪个更有用？
> AI 回答"名字叫张三，年龄二十五岁" vs 返回 `{"name":"张三","age":25}`，哪个程序更容易处理？

**结构化输出的应用场景**：
- 解析用户意图 → 触发不同业务逻辑
- 提取文章关键信息 → 存入数据库
- 生成标准化报告 → 前端渲染
- Function Calling 的参数传递

### 1.2 核心知识：Prompt 引导 JSON 输出（15 分钟）

**方法一：直接指令法**
```python
import os
from openai import OpenAI

client = OpenAI(
    api_key="your-api-key",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

prompt = """
请分析以下文本，提取联系人信息，只输出 JSON，不要有任何其他文字：
文本：张三，25岁，住在北京，联系电话 13800138000

输出格式：
{
    "name": "...",
    "age": ...,
    "city": "...",
    "phone": "..."
}
"""

response = client.chat.completions.create(
    model="qwen-turbo",
    messages=[{"role": "user", "content": prompt}]
)
print(response.choices[0].message.content)
```

**方法二：Few-Shot 示例法**（给出示例让模型照做）
```python
system_prompt = """你是一个信息提取助手。
每次用户输入文本，你都输出标准 JSON 格式的提取结果。

示例：
输入：李四，30岁，工程师
输出：{"name":"李四","age":30,"job":"工程师"}

输入：王五是一名教师，今年28岁
输出：{"name":"王五","age":28,"job":"教师"}

现在请处理用户输入，只输出 JSON，不加解释。"""
```

**方法三：response_format 参数法（推荐）**
```python
response = client.chat.completions.create(
    model="qwen-turbo",
    messages=[
        {"role": "user", "content": "提取：张三，25岁，北京人，电话13800138000"}
    ],
    response_format={"type": "json_object"}   # 强制 JSON 模式
)
```

### 1.3 JSON 解析与容错处理（10 分钟）

```python
import json

def safe_parse_json(text: str) -> dict | None:
    """安全解析 AI 返回的 JSON 字符串"""
    # 1. 直接尝试解析
    try:
        return json.loads(text)
    except json.JSONDecodeError:
        pass

    # 2. 提取代码块中的 JSON
    import re
    pattern = r'```(?:json)?\s*([\s\S]+?)```'
    match = re.search(pattern, text)
    if match:
        try:
            return json.loads(match.group(1))
        except json.JSONDecodeError:
            pass

    # 3. 提取第一个 {} 块
    match = re.search(r'\{[\s\S]+\}', text)
    if match:
        try:
            return json.loads(match.group())
        except json.JSONDecodeError:
            pass

    return None  # 解析失败返回 None


# 使用示例
result = safe_parse_json(response.choices[0].message.content)
if result:
    print(f"姓名: {result.get('name')}")
    print(f"年龄: {result.get('age')}")
else:
    print("JSON 解析失败，原始内容：", response.choices[0].message.content)
```

### 1.4 课堂练习（10 分钟）

**练习 1**：写一个 Prompt，让 AI 读取一段商品描述，提取商品名称、价格、颜色、库存数量，输出 JSON。

**练习 2**：调用 API 解析以下三段文字，用 `safe_parse_json` 解析结果，打印每件商品信息：
```
1. 红色T恤，小码，售价89元，库存200件
2. 蓝色牛仔裤，M码，售价179元，库存50件
3. 白色运动鞋，43码，售价299元，库存30件
```

### 1.5 课后作业

编写一个"简历解析器"：
- 输入：一段文字简历
- 输出：JSON 格式，包含姓名、学校、专业、工作年限、技能列表（数组）
- 要求：处理解析失败的情况，打印友好提示

---

## 第 2 课：JSON Schema 约束与校验

**课时**：45 分钟
**目标**：掌握 JSON Schema 定义方法，用 Schema 精确约束 AI 输出内容

### 2.1 什么是 JSON Schema（8 分钟）

**类比**：JSON Schema 就是 JSON 数据的"表格表头设计"，规定每一列叫什么、是什么类型、是否必填。

```python
# 没有 Schema 的问题：AI 可能返回不一致的字段
# 某次返回：{"name":"张三", "age":25}
# 另一次返回：{"姓名":"张三", "年龄":"25岁"}  ← 字段名变了，类型也变了！

# 有了 Schema，输出格式受到约束
user_schema = {
    "type": "object",
    "properties": {
        "name":  {"type": "string",  "description": "用户姓名"},
        "age":   {"type": "integer", "description": "年龄，必须是整数"},
        "city":  {"type": "string",  "description": "所在城市"},
        "score": {"type": "number",  "description": "评分，0-100的浮点数"}
    },
    "required": ["name", "age"]  # 这两个字段必须有
}
```

### 2.2 Schema 核心字段详解（12 分钟）

```python
# 完整 Schema 示例：订单信息
order_schema = {
    "type": "object",
    "properties": {
        "order_id": {
            "type": "string",
            "description": "订单编号，如 ORD-20240101-001"
        },
        "customer": {
            "type": "string",
            "description": "客户姓名"
        },
        "items": {
            "type": "array",            # 数组类型
            "description": "购买的商品列表",
            "items": {                  # 数组每个元素的格式
                "type": "object",
                "properties": {
                    "product": {"type": "string"},
                    "quantity": {"type": "integer"},
                    "price":    {"type": "number"}
                },
                "required": ["product", "quantity", "price"]
            }
        },
        "total":  {"type": "number",  "description": "订单总金额"},
        "status": {
            "type": "string",
            "enum": ["待支付", "已支付", "已发货", "已完成", "已取消"],  # 枚举限制
            "description": "订单状态"
        },
        "priority": {
            "type": "boolean",
            "description": "是否加急订单"
        }
    },
    "required": ["order_id", "customer", "items", "total", "status"]
}
```

### 2.3 把 Schema 注入 Prompt（10 分钟）

```python
import json

def extract_with_schema(text: str, schema: dict, model: str = "qwen-turbo") -> dict:
    """用 Schema 约束提取结构化信息"""

    schema_str = json.dumps(schema, ensure_ascii=False, indent=2)

    prompt = f"""请从以下文本中提取信息，严格按照 JSON Schema 输出，只输出 JSON 不加任何说明：

JSON Schema：
{schema_str}

待提取文本：
{text}

注意：
1. 只输出符合 Schema 的 JSON
2. required 字段必须存在
3. enum 字段只能使用给定的值"""

    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"}
    )
    return json.loads(response.choices[0].message.content)


# 测试
text = "客户王小明下单了3件红色T恤（每件89元）和1条牛仔裤（179元），已支付，订单号ORD-001"
result = extract_with_schema(text, order_schema)
print(json.dumps(result, ensure_ascii=False, indent=2))
```

### 2.4 Schema 校验：用 jsonschema 库（10 分钟）

```python
# pip install jsonschema
from jsonschema import validate, ValidationError

def validate_ai_output(data: dict, schema: dict) -> tuple[bool, str]:
    """校验 AI 输出是否符合 Schema"""
    try:
        validate(instance=data, schema=schema)
        return True, "校验通过"
    except ValidationError as e:
        return False, f"校验失败：{e.message}"


# 完整流程：提取 + 校验
result = extract_with_schema(text, order_schema)
is_valid, msg = validate_ai_output(result, order_schema)

if is_valid:
    print("提取成功！")
    print(f"客户：{result['customer']}")
    print(f"总金额：{result['total']}")
    for item in result.get('items', []):
        print(f"  - {item['product']} × {item['quantity']} = {item['price'] * item['quantity']}元")
else:
    print(f"数据不合规：{msg}")
```

### 2.5 课堂练习（5 分钟）

设计一个"用户评论分析" Schema，包含：
- 情感倾向（正面/负面/中性）
- 评分（1-5 的整数）
- 关键词（字符串数组）
- 是否需要人工处理（布尔值）

用真实评论测试，输出分析结果。

### 2.6 课后作业

实现一个"日程提取器"：
- 输入：一段话（"明天下午三点在会议室A开产品评审会，记得带上笔记本"）
- Schema 包含：事件名称、日期时间（字符串）、地点、参与者（数组）、备注
- 输出 JSON 并校验，校验失败时重试一次

---

## 第 3 课：XML 结构化输出

**课时**：45 分钟
**目标**：掌握 XML 格式输出的 Prompt 技巧和解析方法，了解适用场景

### 3.1 XML vs JSON：各有用武之地（8 分钟）

| 维度 | JSON | XML |
|------|------|-----|
| 可读性 | 简洁 | 冗长但语义清晰 |
| 嵌套 | 自然 | 层次分明 |
| 属性 | 不支持 | 支持标签属性 |
| 适用场景 | API 传参、数据交换 | 配置文件、文档格式、Prompt 结构化 |
| AI Prompt 中 | 返回数据 | **组织复杂 Prompt 结构**（重点！） |

**重点**：XML 在 AI 领域更多用于**组织 Prompt 本身的结构**（如 Claude 的推荐做法），而不仅是让 AI 输出 XML。

### 3.2 让 AI 输出 XML（12 分钟）

```python
xml_prompt = """
请分析这段产品评论，以 XML 格式输出，只输出 XML 不要有其他文字：

评论：这款手机屏幕很清晰，拍照效果也不错，就是电池续航差了点，价格偏贵。

要求输出格式：
<review>
  <sentiment>正面/负面/中性/混合</sentiment>
  <score>1-5</score>
  <pros>
    <item>优点1</item>
    <item>优点2</item>
  </pros>
  <cons>
    <item>缺点1</item>
  </cons>
  <summary>一句话总结</summary>
</review>
"""

response = client.chat.completions.create(
    model="qwen-turbo",
    messages=[{"role": "user", "content": xml_prompt}]
)
xml_output = response.choices[0].message.content
print(xml_output)
```

### 3.3 Python 解析 XML（12 分钟）

```python
import xml.etree.ElementTree as ET
import re

def parse_ai_xml(xml_text: str) -> ET.Element | None:
    """安全解析 AI 返回的 XML"""
    # 清理 markdown 代码块
    xml_text = re.sub(r'```(?:xml)?\s*', '', xml_text)
    xml_text = xml_text.replace('```', '').strip()

    try:
        return ET.fromstring(xml_text)
    except ET.ParseError as e:
        print(f"XML 解析失败：{e}")
        return None


def review_to_dict(root: ET.Element) -> dict:
    """将 review XML 转为字典"""
    result = {
        "sentiment": root.findtext("sentiment", ""),
        "score":     int(root.findtext("score", "0")),
        "pros":      [item.text for item in root.findall("pros/item")],
        "cons":      [item.text for item in root.findall("cons/item")],
        "summary":   root.findtext("summary", "")
    }
    return result


# 完整使用
root = parse_ai_xml(xml_output)
if root:
    data = review_to_dict(root)
    print(f"情感：{data['sentiment']}，评分：{data['score']}/5")
    print(f"优点：{', '.join(data['pros'])}")
    print(f"缺点：{', '.join(data['cons'])}")
    print(f"总结：{data['summary']}")
```

### 3.4 用 XML 结构化你的 Prompt（重要技巧，8 分钟）

```python
# 用 XML 标签组织复杂 Prompt，让 AI 更准确理解结构
structured_prompt = """
<task>
  <role>你是一位专业的代码审查工程师</role>
  <input>
    <code language="python">
      def add(a, b):
          return a - b   # 这里有 bug
    </code>
  </input>
  <requirements>
    <item>找出代码中的 bug</item>
    <item>给出修复建议</item>
    <item>评估代码质量（1-10分）</item>
  </requirements>
  <output_format>
    以 JSON 格式返回：{"bug": "...", "fix": "...", "score": 数字}
  </output_format>
</task>
"""

response = client.chat.completions.create(
    model="qwen-turbo",
    messages=[{"role": "user", "content": structured_prompt}],
    response_format={"type": "json_object"}
)
print(json.loads(response.choices[0].message.content))
```

### 3.5 课堂练习（5 分钟）

用 XML 结构化 Prompt，让 AI 分析一段英文新闻，输出 JSON 格式的结果（标题、作者、时间、摘要、关键词列表）。

### 3.6 课后作业

实现一个"简历 XML 解析器"：
- 让 AI 将文字简历转为 XML 格式（包含个人信息、教育经历、工作经历标签）
- 用 Python 解析 XML，提取所有工作经历，计算总工作年数

---

## 第 4 课：System Prompt 与角色设定

**课时**：45 分钟
**目标**：深入理解 role 机制，掌握高效 System Prompt 的写法，实现持久角色人设

### 4.1 三种消息角色解析（10 分钟）

```python
# Qwen / OpenAI 消息结构
messages = [
    {
        "role": "system",     # 系统指令：设定 AI 的角色、规则、约束
        "content": "你是一个专业的财务顾问，只回答理财相关问题。"
    },
    {
        "role": "user",       # 用户输入
        "content": "我有10万元，怎么理财？"
    },
    {
        "role": "assistant",  # AI 历史回复（多轮对话时填入）
        "content": "建议您先建立3-6个月的紧急备用金..."
    },
    {
        "role": "user",
        "content": "紧急备用金放在哪里合适？"
    }
]
```

**三个角色的权重**：`system > user > assistant`
- `system`：最高权威，AI 会优先遵守
- `user`：普通指令
- `assistant`：历史记录，建立上下文

### 4.2 System Prompt 设计四要素（15 分钟）

```python
# 要素1：角色定义（Who）
# 要素2：行为准则（Rules）
# 要素3：输出格式（Format）
# 要素4：边界限制（Boundaries）

system_prompt_template = """
## 角色
你是「学小智」，一个专为初学者设计的 Python 编程助教。

## 行为准则
1. 用简单易懂的语言解释技术概念，避免术语堆砌
2. 每次回答必须包含一个可运行的代码示例
3. 鼓励学员，当学员犯错时给予正面引导
4. 回答要简洁，每条解释不超过3句话

## 输出格式
- 解释部分：2-3句话
- 代码示例：用 ```python 代码块包裹
- 延伸思考：一个引导性问题

## 边界限制
- 只回答 Python 相关问题
- 不涉及政治、宗教等无关话题
- 遇到超出范围的问题，礼貌地引导回 Python 学习
"""

response = client.chat.completions.create(
    model="qwen-turbo",
    messages=[
        {"role": "system", "content": system_prompt_template},
        {"role": "user",   "content": "什么是列表推导式？"}
    ]
)
print(response.choices[0].message.content)
```

### 4.3 角色持久化：用类封装（12 分钟）

```python
class AIRole:
    """可持久化的 AI 角色"""

    def __init__(self, name: str, system_prompt: str, model: str = "qwen-turbo"):
        self.name = name
        self.system_prompt = system_prompt
        self.model = model
        self.history: list[dict] = []  # 对话历史

    def chat(self, user_input: str) -> str:
        """发送消息，保留上下文"""
        self.history.append({"role": "user", "content": user_input})

        messages = [{"role": "system", "content": self.system_prompt}] + self.history

        response = client.chat.completions.create(
            model=self.model,
            messages=messages
        )
        reply = response.choices[0].message.content
        self.history.append({"role": "assistant", "content": reply})
        return reply

    def reset(self):
        """重置对话历史，保留角色设定"""
        self.history = []
        print(f"[{self.name}] 对话历史已清空")

    def get_history_count(self) -> int:
        return len(self.history) // 2  # 轮数


# 创建两个不同角色
python_tutor = AIRole(
    name="学小智",
    system_prompt="你是一个友善的Python编程助教，用简单语言教初学者。每次回答要包含代码示例。"
)

career_advisor = AIRole(
    name="职场导师",
    system_prompt="你是一个职场规划顾问，帮助IT从业者制定职业发展计划，回答要实用具体。"
)

# 测试
print(python_tutor.chat("什么是函数？"))
print(career_advisor.chat("我是Python初学者，一年内想找到工作，怎么规划？"))
```

### 4.4 课堂练习（8 分钟）

设计并实现以下两个角色：
1. **严格代码审查官**：发现代码问题时态度严格，要求学员修改后才能通过
2. **温柔学习伙伴**：鼓励式教学，即使代码有错也先肯定亮点

测试：输入同一段有 bug 的代码，对比两个角色的回复风格差异。

### 4.5 课后作业

设计一个"面试官角色"：
- 专门针对 Python 初级岗位面试
- 按顺序提问（基础语法 → OOP → 实际项目）
- 根据学员回答质量给出评分（JSON 格式：`{"score": 分数, "feedback": "评价", "next_question": "下一题"}`）
- 实现 3 轮问答后给出综合评估

---

## 第 5 课：多轮对话与上下文管理

**课时**：45 分钟
**目标**：深入理解上下文窗口机制，实现健壮的多轮对话管理

### 5.1 上下文窗口原理（8 分钟）

```
每次 API 调用 = 重新发送全部历史记录
─────────────────────────────────
第1轮：[system] + [user1] + [ai1]
第2轮：[system] + [user1] + [ai1] + [user2] + [ai2]
第3轮：[system] + [user1] + [ai1] + [user2] + [ai2] + [user3] + [ai3]
                                               ↑ 历史越来越长，token 消耗越来越多
```

**Token 限制提示**：
- qwen-turbo：8192 tokens（输入+输出）
- qwen-plus：131072 tokens
- qwen-max：8192 tokens

### 5.2 对话管理器实现（20 分钟）

```python
class ConversationManager:
    """多轮对话管理器"""

    def __init__(
        self,
        system_prompt: str,
        model: str = "qwen-turbo",
        max_history: int = 10,       # 最多保留几轮历史
        max_tokens: int = 4000       # 历史区预留 token 上限（粗略估算）
    ):
        self.system_prompt = system_prompt
        self.model = model
        self.max_history = max_history
        self.max_tokens = max_tokens
        self.history: list[dict] = []
        self.turn_count = 0

    def _estimate_tokens(self, text: str) -> int:
        """粗略估算 token 数（中文约1.5字/token，英文约4字/token）"""
        chinese_count = sum(1 for c in text if '\u4e00' <= c <= '\u9fff')
        other_count = len(text) - chinese_count
        return int(chinese_count / 1.5 + other_count / 4)

    def _trim_history(self):
        """修剪历史记录：超过限制则删除最早的对话轮"""
        # 先按轮数限制
        while len(self.history) > self.max_history * 2:
            self.history.pop(0)  # 删除最早的 user
            self.history.pop(0)  # 删除对应的 assistant

        # 再按 token 估算限制
        total_tokens = sum(self._estimate_tokens(m["content"]) for m in self.history)
        while total_tokens > self.max_tokens and len(self.history) >= 2:
            removed_user = self.history.pop(0)
            removed_ai   = self.history.pop(0)
            total_tokens -= (
                self._estimate_tokens(removed_user["content"]) +
                self._estimate_tokens(removed_ai["content"])
            )

    def chat(self, user_input: str, verbose: bool = False) -> str:
        """发送消息，自动管理上下文"""
        self.turn_count += 1
        self.history.append({"role": "user", "content": user_input})

        messages = [{"role": "system", "content": self.system_prompt}] + self.history

        if verbose:
            total_msg_tokens = sum(self._estimate_tokens(m["content"]) for m in messages)
            print(f"[轮次 {self.turn_count}] 历史轮数: {len(self.history)//2}, 预计 token: ~{total_msg_tokens}")

        response = client.chat.completions.create(
            model=self.model,
            messages=messages
        )
        reply = response.choices[0].message.content
        self.history.append({"role": "assistant", "content": reply})

        # 添加后再修剪（保证本轮完整）
        self._trim_history()
        return reply

    def inject_context(self, context: str):
        """注入背景信息（不占用对话轮次）"""
        # 用 system 角色在历史中插入一条上下文
        self.history.append({
            "role": "user",
            "content": f"[背景信息，请记住：{context}]"
        })
        self.history.append({
            "role": "assistant",
            "content": "好的，我已记录这些背景信息。"
        })

    def get_summary_prompt(self) -> str:
        """生成对话摘要 prompt"""
        history_text = "\n".join(
            f"{'用户' if m['role']=='user' else 'AI'}: {m['content']}"
            for m in self.history
        )
        return f"请将以下对话总结为3-5句话的摘要：\n\n{history_text}"


# 使用演示
conv = ConversationManager(
    system_prompt="你是一个学习计划助手，帮助用户制定个性化学习路线。",
    max_history=6
)

print(conv.chat("我想学习机器学习", verbose=True))
print(conv.chat("我有Python基础，每天能学2小时", verbose=True))
print(conv.chat("三个月后要参加面试，重点是什么？", verbose=True))
```

### 5.3 对话状态管理（12 分钟）

```python
from enum import Enum
from dataclasses import dataclass, field
from datetime import datetime

class ConversationState(Enum):
    IDLE      = "空闲"
    ACTIVE    = "活跃"
    WAITING   = "等待用户"
    SUMMARIZING = "摘要中"
    ENDED     = "已结束"

@dataclass
class ConversationSession:
    """对话会话（带状态追踪）"""
    session_id: str
    role_name: str
    created_at: str = field(default_factory=lambda: datetime.now().strftime("%Y-%m-%d %H:%M"))
    state: ConversationState = ConversationState.IDLE
    turn_count: int = 0
    tags: list[str] = field(default_factory=list)

    def start(self):
        self.state = ConversationState.ACTIVE
        print(f"[{self.role_name}] 会话 {self.session_id} 开始")

    def end(self):
        self.state = ConversationState.ENDED
        print(f"[{self.role_name}] 会话结束，共 {self.turn_count} 轮")

    def __str__(self):
        return f"Session({self.session_id}, {self.role_name}, {self.state.value}, {self.turn_count}轮)"
```

### 5.4 课堂练习（5 分钟）

实现一个"健忘测试"：
1. 创建 `max_history=2` 的对话管理器
2. 告诉 AI：你的名字叫"小明"，你喜欢吃苹果
3. 聊4轮无关话题（把第1轮挤出历史）
4. 再问 AI "你的名字是什么？你喜欢吃什么？"
5. 观察 AI 是否"忘记"了

### 5.5 课后作业

实现一个"购物助手"对话管理器：
- System prompt 设定为超市导购
- 用户可以说"我要买苹果"，AI 记录购物清单
- 实现 `get_cart()` 方法，随时获取 AI 记录的购物清单（JSON 格式）
- 支持"清空购物车"指令

---

## 第 6 课：多角色对话系统设计

**课时**：45 分钟
**目标**：构建可动态切换角色的多角色对话系统

### 6.1 多角色系统架构（8 分钟）

```
┌─────────────────────────────────────┐
│           用户请求                   │
└───────────────┬─────────────────────┘
                │
        ┌───────▼──────┐
        │  角色路由器   │  ← 判断该用哪个角色
        └───────┬──────┘
                │
    ┌───────────┼───────────┐
    ▼           ▼           ▼
┌───────┐  ┌────────┐  ┌─────────┐
│ 客服  │  │ 技术   │  │ 销售    │
│ 角色  │  │ 专家   │  │ 顾问    │
└───────┘  └────────┘  └─────────┘
    │           │           │
    └───────────┴───────────┘
                │
        ┌───────▼──────┐
        │  统一回复管   │
        │     道       │
        └──────────────┘
```

### 6.2 多角色管理系统实现（25 分钟）

```python
class RoleManager:
    """多角色管理器"""

    def __init__(self, model: str = "qwen-turbo"):
        self.model = model
        self.roles: dict[str, dict] = {}        # 角色库
        self.role_histories: dict[str, list] = {}  # 各角色独立历史
        self.current_role: str | None = None    # 当前角色

    def register_role(self, role_id: str, name: str, system_prompt: str, description: str = ""):
        """注册一个角色"""
        self.roles[role_id] = {
            "name":          name,
            "system_prompt": system_prompt,
            "description":   description
        }
        self.role_histories[role_id] = []
        print(f"角色 [{name}] 注册成功 (id: {role_id})")

    def switch_role(self, role_id: str, keep_history: bool = False):
        """切换当前角色"""
        if role_id not in self.roles:
            raise ValueError(f"角色 {role_id} 不存在")

        old_role = self.current_role
        self.current_role = role_id

        if not keep_history:
            # 切换时可选择是否清空该角色的历史
            pass

        role_name = self.roles[role_id]["name"]
        print(f"角色切换：{self.roles[old_role]['name'] if old_role else '无'} → {role_name}")

    def chat(self, user_input: str) -> str:
        """用当前角色对话"""
        if self.current_role is None:
            raise RuntimeError("未设置当前角色，请先调用 switch_role()")

        role = self.roles[self.current_role]
        history = self.role_histories[self.current_role]

        history.append({"role": "user", "content": user_input})

        messages = [{"role": "system", "content": role["system_prompt"]}] + history

        response = client.chat.completions.create(
            model=self.model,
            messages=messages
        )
        reply = response.choices[0].message.content
        history.append({"role": "assistant", "content": reply})
        return reply

    def auto_route(self, user_input: str) -> str:
        """自动路由：让 AI 判断该用哪个角色，然后切换并回答"""
        role_descriptions = "\n".join(
            f"- {rid}: {info['name']} - {info['description']}"
            for rid, info in self.roles.items()
        )

        routing_prompt = f"""根据用户输入，选择最合适的角色ID来回答。
只输出角色ID，不要任何其他内容。

可用角色：
{role_descriptions}

用户输入：{user_input}
"""
        routing_response = client.chat.completions.create(
            model=self.model,
            messages=[{"role": "user", "content": routing_prompt}]
        )
        role_id = routing_response.choices[0].message.content.strip()

        # 容错：如果返回的不是有效 role_id，用第一个角色
        if role_id not in self.roles:
            role_id = list(self.roles.keys())[0]

        self.switch_role(role_id)
        return self.chat(user_input)

    def list_roles(self):
        """显示所有角色"""
        print("\n=== 已注册角色 ===")
        for rid, info in self.roles.items():
            marker = " ← 当前" if rid == self.current_role else ""
            print(f"  [{rid}] {info['name']}: {info['description']}{marker}")


# 构建一个客服系统
manager = RoleManager()

manager.register_role(
    "tech",
    "技术支持",
    "你是专业技术支持工程师，解答产品技术问题，答案要具体准确。",
    "处理技术问题、bug 报告、使用教程"
)

manager.register_role(
    "sales",
    "销售顾问",
    "你是热情的销售顾问，了解产品优势，引导用户购买，回答要有亲和力。",
    "产品介绍、价格咨询、购买建议"
)

manager.register_role(
    "complaint",
    "投诉处理",
    "你是有同理心的投诉处理专员，先安抚情绪，再解决问题，语气要温柔耐心。",
    "处理投诉、退款申请、纠纷协调"
)

manager.list_roles()

# 自动路由测试
test_inputs = [
    "我的软件安装后报错，提示 dll 文件缺失",
    "你们产品多少钱？有没有优惠？",
    "我买了你们产品根本不能用，要退款！"
]

for user_input in test_inputs:
    print(f"\n用户：{user_input}")
    reply = manager.auto_route(user_input)
    print(f"AI [{manager.roles[manager.current_role]['name']}]：{reply[:100]}...")
```

### 6.3 课堂练习（12 分钟）

构建"学习助手系统"，注册三个角色：
- 知识讲解老师（详细解释概念）
- 出题考官（出判断题/选择题考察理解）
- 学习规划师（制定复习计划）

实现手动切换功能，模拟一段完整的学习流程：先学概念→再做题→再规划。

---

## 第 7 课：对话记忆与摘要压缩

**课时**：45 分钟
**目标**：解决长对话 token 超限问题，实现对话摘要与记忆注入

### 7.1 长对话的三种策略（5 分钟）

| 策略 | 方法 | 优点 | 缺点 |
|------|------|------|------|
| 滑动窗口 | 只保留最近 N 轮 | 简单 | 会丢失早期重要信息 |
| 摘要压缩 | 定期总结历史 | 保留信息精华 | 需要额外 API 调用 |
| 向量记忆 | 相似度检索相关历史 | 精准召回 | 需要向量数据库 |

### 7.2 摘要压缩实现（20 分钟）

```python
class SummaryConversationManager:
    """带摘要压缩的对话管理器"""

    def __init__(
        self,
        system_prompt: str,
        model: str = "qwen-turbo",
        compress_every: int = 6,   # 每 6 轮压缩一次
        keep_recent: int = 3       # 压缩后保留最近 3 轮原文
    ):
        self.system_prompt = system_prompt
        self.model = model
        self.compress_every = compress_every
        self.keep_recent = keep_recent
        self.history: list[dict] = []
        self.summary: str = ""     # 历史摘要（压缩的结果）
        self.turn_count = 0

    def _compress(self):
        """压缩历史：把早期历史总结成摘要"""
        print("\n[系统] 正在压缩对话历史...")

        # 准备要压缩的部分（保留最近 keep_recent 轮）
        keep_count = self.keep_recent * 2
        to_compress = self.history[:-keep_count] if len(self.history) > keep_count else []
        recent = self.history[-keep_count:] if len(self.history) > keep_count else self.history

        if not to_compress:
            return  # 没有需要压缩的内容

        # 生成新摘要
        history_text = "\n".join(
            f"{'用户' if m['role']=='user' else 'AI'}: {m['content']}"
            for m in to_compress
        )

        existing_summary = f"已有摘要：\n{self.summary}\n\n" if self.summary else ""

        compress_prompt = f"""{existing_summary}请将以下对话补充摘要到已有摘要中，保留关键事实、用户偏好、重要决策：

{history_text}

要求：
1. 摘要控制在200字以内
2. 用第三人称描述（"用户表示..."）
3. 突出关键信息"""

        response = client.chat.completions.create(
            model=self.model,
            messages=[{"role": "user", "content": compress_prompt}]
        )
        self.summary = response.choices[0].message.content

        # 用摘要替换旧历史
        self.history = recent
        print(f"[系统] 压缩完成，历史从 {len(to_compress)+len(recent)} 条压缩至 {len(recent)} 条")
        print(f"[摘要] {self.summary[:80]}...")

    def chat(self, user_input: str) -> str:
        """对话，自动触发摘要压缩"""
        self.turn_count += 1
        self.history.append({"role": "user", "content": user_input})

        # 构建消息：system + 摘要（如有）+ 近期历史
        messages = [{"role": "system", "content": self.system_prompt}]

        if self.summary:
            messages.append({
                "role": "system",
                "content": f"[对话摘要 - 之前聊过的内容]\n{self.summary}"
            })

        messages.extend(self.history)

        response = client.chat.completions.create(
            model=self.model,
            messages=messages
        )
        reply = response.choices[0].message.content
        self.history.append({"role": "assistant", "content": reply})

        # 达到压缩阈值时触发压缩
        if self.turn_count % self.compress_every == 0:
            self._compress()

        return reply


# 测试长对话
mgr = SummaryConversationManager(
    system_prompt="你是一个旅游规划师，帮用户规划旅行。记住用户提到的偏好和需求。",
    compress_every=4,
    keep_recent=2
)

conversations = [
    "我想去日本旅游，喜欢安静的地方，不喜欢人多的景点",
    "我的预算大概是1万元，行程10天",
    "我对历史文化很感兴趣，喜欢寺庙和神社",
    "我不能吃海鲜，对花粉过敏",
    "出发时间大概是明年三月",
    "请给我推荐一个详细的10天行程"  # 这时已经压缩过了，看看 AI 还记得多少
]

for msg in conversations:
    print(f"\n用户：{msg}")
    reply = mgr.chat(msg)
    print(f"AI：{reply[:120]}...")
```

### 7.3 关键信息提取与注入（12 分钟）

```python
def extract_key_facts(conversation_history: list[dict], client) -> dict:
    """从对话中提取关键事实，用于后续会话"""
    history_text = "\n".join(
        f"{'用户' if m['role']=='user' else 'AI'}: {m['content']}"
        for m in conversation_history
    )

    prompt = f"""从以下对话中提取关键信息，输出 JSON 格式：

{history_text}

输出格式：
{{
    "user_name": "用户姓名（如果提到）",
    "preferences": ["偏好列表"],
    "constraints": ["限制条件列表"],
    "goals": ["目标列表"],
    "important_facts": ["其他重要事实"]
}}"""

    response = client.chat.completions.create(
        model="qwen-turbo",
        messages=[{"role": "user", "content": prompt}],
        response_format={"type": "json_object"}
    )
    return json.loads(response.choices[0].message.content)
```

### 7.4 课堂练习（8 分钟）

对比实验：
1. 用普通 `ConversationManager`（max_history=4）进行10轮对话
2. 用 `SummaryConversationManager` 进行同样10轮对话
3. 第10轮问同一个问题，对比两种管理器的回答质量

### 7.5 课后作业

为第4课的"面试官角色"加入摘要压缩：
- 面试结束后，自动生成候选人档案（JSON）
- 包含：候选人表现、知识掌握程度、建议录用与否、薄弱项

---

## 第 8 课：综合项目——AI 助理平台

**课时**：45 分钟
**目标**：整合前7课所有技能，构建完整的多功能 AI 助理平台

### 8.1 项目介绍与架构设计（8 分钟）

**项目：PersonalAI 个人 AI 助理**

功能模块：
1. **知识问答**（结合 JSON 结构化输出）
2. **任务管理**（结构化提取任务信息）
3. **写作助手**（多风格角色切换）
4. **学习顾问**（带摘要压缩的长对话）

```
PersonalAI
├── main.py              # 主程序入口（命令行交互）
├── core/
│   ├── role_manager.py  # 多角色管理（第6课）
│   ├── conv_manager.py  # 对话管理+摘要压缩（第5、7课）
│   └── extractor.py     # 结构化信息提取（第1、2课）
└── roles/
    ├── qa_role.py       # 知识问答角色
    ├── task_role.py     # 任务管理角色
    ├── writing_role.py  # 写作助手角色
    └── tutor_role.py    # 学习顾问角色
```

### 8.2 核心代码整合（25 分钟）

```python
# personal_ai.py - 完整项目核心

import json
import os
from openai import OpenAI
from datetime import datetime

client = OpenAI(
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

# ── 角色定义 ────────────────────────────────────────────────
ROLES = {
    "qa": {
        "name": "知识助手",
        "prompt": """你是一个知识渊博的问答助手。
回答问题时，输出 JSON 格式：
{"answer": "回答内容", "confidence": "高/中/低", "related_topics": ["相关话题1", "相关话题2"]}""",
        "output": "json"
    },
    "task": {
        "name": "任务管家",
        "prompt": """你是一个任务管理助手。
当用户描述任务时，提取任务信息输出 JSON：
{"task_name": "任务名", "deadline": "截止日期或null", "priority": "高/中/低", "tags": ["标签"]}
当用户查询任务列表时，以文字形式回复。""",
        "output": "json"
    },
    "writing": {
        "name": "写作助手",
        "prompt": """你是一个专业写作助手，擅长各种文体。
用户可以指定写作风格（正式/轻松/文艺/幽默）。
默认风格：轻松自然。""",
        "output": "text"
    },
    "tutor": {
        "name": "学习导师",
        "prompt": """你是一位耐心细致的学习导师，擅长 Python 和 AI 领域。
根据学员水平调整讲解深度，多用类比和例子。
遇到学员不理解时，换一种方式解释。""",
        "output": "text"
    }
}

class PersonalAI:
    def __init__(self):
        self.current_role = "qa"
        self.histories: dict[str, list] = {role_id: [] for role_id in ROLES}
        self.summaries: dict[str, str] = {role_id: "" for role_id in ROLES}
        self.tasks: list[dict] = []    # 任务列表
        self.turn_counts: dict[str, int] = {role_id: 0 for role_id in ROLES}

    def switch(self, role_id: str):
        if role_id not in ROLES:
            print(f"无效角色ID，可选：{', '.join(ROLES.keys())}")
            return
        self.current_role = role_id
        print(f"✓ 已切换到【{ROLES[role_id]['name']}】")

    def chat(self, user_input: str) -> str:
        role = ROLES[self.current_role]
        history = self.histories[self.current_role]
        summary = self.summaries[self.current_role]

        history.append({"role": "user", "content": user_input})

        messages = [{"role": "system", "content": role["prompt"]}]
        if summary:
            messages.append({"role": "system", "content": f"[历史摘要]\n{summary}"})
        messages.extend(history[-12:])  # 最近6轮

        kwargs = {"model": "qwen-turbo", "messages": messages}
        if role["output"] == "json":
            kwargs["response_format"] = {"type": "json_object"}

        response = client.chat.completions.create(**kwargs)
        reply = response.choices[0].message.content
        history.append({"role": "assistant", "content": reply})

        # 任务管理：解析并保存任务
        if self.current_role == "task":
            try:
                task_data = json.loads(reply)
                if "task_name" in task_data:
                    task_data["created_at"] = datetime.now().strftime("%m-%d %H:%M")
                    task_data["id"] = len(self.tasks) + 1
                    self.tasks.append(task_data)
            except (json.JSONDecodeError, KeyError):
                pass

        self.turn_counts[self.current_role] += 1
        return reply

    def show_tasks(self):
        if not self.tasks:
            print("暂无任务")
            return
        print("\n=== 任务列表 ===")
        for t in self.tasks:
            deadline = t.get('deadline') or '无截止日期'
            print(f"  [{t['id']}] {t['task_name']} | {t.get('priority','中')}优先 | {deadline}")

    def status(self):
        print(f"\n当前角色：【{ROLES[self.current_role]['name']}】")
        print(f"任务数量：{len(self.tasks)}")
        for rid, count in self.turn_counts.items():
            if count > 0:
                print(f"  {ROLES[rid]['name']}: {count} 轮对话")


def main():
    ai = PersonalAI()
    print("PersonalAI 启动！输入 /help 查看命令\n")

    COMMANDS = {
        "/qa":      ("切换知识助手",    lambda: ai.switch("qa")),
        "/task":    ("切换任务管家",    lambda: ai.switch("task")),
        "/write":   ("切换写作助手",    lambda: ai.switch("writing")),
        "/tutor":   ("切换学习导师",    lambda: ai.switch("tutor")),
        "/tasks":   ("查看任务列表",    lambda: ai.show_tasks()),
        "/status":  ("查看系统状态",    lambda: ai.status()),
        "/quit":    ("退出",           lambda: exit(0)),
    }

    while True:
        user_input = input(f"\n[{ROLES[ai.current_role]['name']}] > ").strip()
        if not user_input:
            continue

        if user_input in COMMANDS:
            desc, action = COMMANDS[user_input]
            action()
            continue

        if user_input == "/help":
            for cmd, (desc, _) in COMMANDS.items():
                print(f"  {cmd:12} {desc}")
            continue

        reply = ai.chat(user_input)

        # 格式化输出
        if ROLES[ai.current_role]["output"] == "json":
            try:
                data = json.loads(reply)
                print(json.dumps(data, ensure_ascii=False, indent=2))
            except json.JSONDecodeError:
                print(reply)
        else:
            print(f"\n{reply}")


if __name__ == "__main__":
    main()
```

### 8.3 项目演示与测试（7 分钟）

**演示脚本**：
```
1. /qa    → 问："Python 的 GIL 是什么？"
2. /task  → 说："今晚10点前完成作业，高优先级"
3. /write → 说："帮我写一段有趣的代码注释，幽默风格"
4. /tutor → 说："我搞不懂装饰器，帮我解释"
5. /tasks → 查看任务列表
6. /status → 查看使用状态
```

### 8.4 拓展思考（5 分钟）

**下一步可以加的功能**：
- 数据持久化（把对话历史保存到文件/数据库）
- Web UI（Flask/FastAPI + 前端）
- 与 Function Calling 结合（AI 自动调用工具）
- 语音输入输出（TTS/STT）

### 8.5 课后大作业（综合项目）

在 PersonalAI 基础上，**二选一**：

**选项 A：记账助手**
- 添加"账单管理"角色
- 用 JSON Schema 提取消费记录（金额、类别、时间、备注）
- 支持"本月消费汇总"查询（按类别统计）
- 输出 JSON 格式的月度报告

**选项 B：情绪日记**
- 添加"日记助手"角色
- 解析用户日记，提取情绪标签（开心/焦虑/平静/悲伤等）
- 用 XML Prompt 组织分析结构，输出 JSON 情绪报告
- 支持查看"本周情绪趋势"

---

## 课程总结

### 8 课时知识树

```
结构化输出与对话管理
│
├── 结构化输出（第1-3课）
│   ├── JSON 输出：Prompt 诱导 + response_format
│   ├── JSON Schema：字段约束 + jsonschema 校验
│   └── XML 输出：解析 + 用 XML 组织 Prompt
│
├── 角色与对话管理（第4-5课）
│   ├── 三种消息角色：system / user / assistant
│   ├── System Prompt 四要素设计
│   ├── 上下文窗口原理与 token 限制
│   └── ConversationManager：历史管理 + token 修剪
│
├── 多角色系统（第6课）
│   ├── RoleManager：注册、切换、独立历史
│   └── 自动路由：AI 判断用哪个角色
│
└── 记忆优化（第7课）
    ├── 三种长对话策略对比
    └── SummaryConversationManager：定期摘要压缩
```

### 能力矩阵评估

| 技能点 | 入门 | 掌握 | 进阶 |
|--------|------|------|------|
| JSON 结构化输出 | 会用 Prompt 引导 | + response_format + 容错解析 | + Schema 约束 + 校验 |
| XML 结构化 | 会让 AI 输出 XML | + Python 解析 | + 用 XML 组织 Prompt |
| 角色设定 | 写 system prompt | + 四要素规范 + AIRole 类 | + 多角色 + 自动路由 |
| 上下文管理 | 知道要传 history | + 滑动窗口修剪 | + 摘要压缩 + 状态追踪 |
| 综合项目 | 单功能对话 | + 多角色系统 | + 完整平台 |

---

*教案版本：v1.0 | 适用课程：W4 大模型 API 调用 | 课时：8 × 45 min*
